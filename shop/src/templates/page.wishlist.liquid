<div class="page-width pageContent wishPage ais-page"> 
  <div class="grid">
    <div class="grid__item one-whole">
      {% comment %}
      <div class="section-header">
        <h1>{{ page.title }}</h1>
      </div>
      <div class="rte">
        {{ page.content }}
      </div>
      {% endcomment %} 
      <div id='wishlist-collection'></div>
    </div>
  </div>
</div>
{% render 'wishlist-templates' %}
{{ 'option_selection.js' | shopify_asset_url | script_tag }}